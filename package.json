{
  "name": "Cours1",
  "version": "1.0.0",
  "description": "Super cours",
  "main": "index.js",
  "repository": "https://github.com/ptondereauSopSte/DFS.git",
  "author": "Paul Tondereau <paul.tondereau@yahoo.fr>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp src/index.html dist && yarn run postcss -o dist/main.css src/tailwind.pcss && yarn run postcss -o src/main.css src/tailwind.pcss"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^1.3.0",
    "postcss": "^7.0.18",
    "postcss-cli": "^6.1.3",
    "postcss-import": "^12.0.1",
    "tailwindcss": "^1.1.2"
  }
}
